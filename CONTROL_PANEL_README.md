# MegaRover3 控制面板使用指南

## 快速启动

### 方法1: 使用快捷脚本
```bash
~/megarover3_panel
```

### 方法2: 使用ROS2命令
```bash
cd ~/Code/Demo8
source install/setup.bash
ros2 run megarover3_navigation control_panel.py
```

### 方法3: 直接运行Python脚本
```bash
cd ~/Code/Demo8
source install/setup.bash
python3 src/megarover3_ros2/megarover3_navigation/scripts/control_panel.py
```

## 功能说明

### 组件控制区域

控制面板左侧显示所有可控制的组件，每个组件包括：

- **状态指示灯**：
  - 🟢 绿色：运行正常
  - 🟡 橙色：部分运行（进程启动但ROS2话题/节点未检测到）
  - ⚫ 灰色：未运行
  - 🔴 红色：错误状态

- **组件列表**：
  1. 底盘 (Micro-ROS)
  2. MID360 激光雷达
  3. D455 前方相机
  4. SLAM (建图)
  5. 导航
  6. 键盘遥控

- **操作按钮**：
  - **启动**：启动该组件（绿色按钮）
  - **停止**：停止该组件（红色按钮）

### 快捷操作

- **🗺️ 一键启动建图**：按顺序启动底盘 → 激光雷达 → 前方相机 → SLAM建图
- **🧭 一键启动导航**：选择地图文件后，按顺序启动底盘 → 激光雷达 → 前方相机 → 导航
- **💾 保存地图**：保存当前建立的地图到maps目录
- **⚠️ 清除所有节点**：强制终止所有ROS2进程（慎用！）

### 日志输出区域

控制面板右侧实时显示所有组件的日志输出：

- **颜色编码**：
  - 🟢 绿色：INFO信息
  - 🟡 黄色：WARN警告
  - 🔴 红色：ERROR错误
  - 🔵 蓝色：DEBUG调试

- **日志控制**：
  - **清除日志**：清空当前所有日志
  - **自动滚动**：开启/关闭自动滚动到最新日志

## 使用流程

### 建图流程

1. 点击 "🗺️ 一键启动建图" 按钮
2. 等待所有组件启动（观察状态指示灯变绿）
3. 使用键盘遥控或手动控制机器人移动
4. 在RViz中观察地图构建
5. 建图完成后，点击 "💾 保存地图"
6. 输入地图名称并保存

### 导航流程

1. 点击 "🧭 一键启动导航" 按钮
2. 选择之前保存的地图文件（在maps目录下）
3. 等待所有组件启动（观察状态指示灯变绿）
4. 在RViz中使用 "2D Goal Pose" 设置目标点
5. 机器人自动导航到目标点

### 手动控制流程

1. 依次启动：底盘 → 激光雷达（可选）
2. 启动"键盘遥控"组件
3. 在键盘遥控终端窗口中使用WASD键控制移动

## 依赖关系

控制面板会自动检查依赖关系：

- **SLAM** 依赖：底盘、激光雷达
- **导航** 依赖：底盘、激光雷达
- **键盘遥控** 依赖：底盘

如果尝试启动的组件依赖未满足，会弹出警告提示。

## 故障排除

### 组件启动失败

1. 检查日志输出区域的错误信息
2. 确认依赖组件已启动
3. 检查硬件连接（USB设备、网络等）
4. 尝试停止组件后重新启动

### 状态指示灯显示橙色

表示进程已启动但ROS2话题或节点未检测到：

- 等待几秒，某些组件需要初始化时间
- 检查日志输出是否有错误信息
- 确认ROS2网络配置正确

### 进程无法停止

1. 尝试再次点击停止按钮
2. 使用 "⚠️ 清除所有节点" 强制终止
3. 手动使用命令行：`pkill -9 -f ros2`

### GUI无法启动

检查依赖是否安装：
```bash
pip3 install PyQt5 psutil PyYAML
```

## 配置文件

组件配置文件位置：
```
src/megarover3_ros2/megarover3_navigation/gui/config/panel_config.yaml
```

可以编辑此文件来：
- 修改组件启动命令
- 添加新的组件
- 修改依赖关系
- 配置ROS2话题/节点检查

## 技术细节

- **进程管理**：使用Python subprocess模块管理ROS2进程
- **日志捕获**：每个组件使用独立线程捕获stdout/stderr
- **ROS2监控**：独立线程运行rclpy进行话题和节点检测
- **UI框架**：PyQt5图形界面
- **状态更新**：1秒刷新间隔

## 限制和注意事项

1. **日志行数限制**：最多保留1000行日志，防止内存溢出
2. **停止超时**：进程停止等待10秒，超时后强制杀死
3. **并发限制**：不建议同时启动多个相同组件
4. **权限要求**：需要有权限访问USB设备和网络端口

## 开发者信息

- **版本**：1.0.0
- **包名**：megarover3_navigation
- **代码位置**：`src/megarover3_ros2/megarover3_navigation/gui/`

## 反馈和问题

如有问题或建议，请查看日志输出并报告错误信息。
