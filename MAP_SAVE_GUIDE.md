# 地图保存完整指南

## 📋 目录
- [为什么需要停止 SLAM](#为什么需要停止-slam)
- [保存流程说明](#保存流程说明)
- [使用方法](#使用方法)
- [常见问题](#常见问题)

---

## 🔴 为什么需要停止 SLAM？

### 数据完整性问题

#### 1. **文件写入冲突**
```
FastLIO 运行时：
  map_latest.pcd ← 持续写入新点云数据
       ↓
  复制操作同时进行
       ↓
  结果：可能得到不完整/损坏的文件
```

**实际影响**：
- 复制到一半的点云文件
- 文件大小不正确
- 加载时崩溃或数据缺失

#### 2. **时间不一致**
```
2D 地图（/map）      在 t1 时刻保存
3D 点云（.pcd）      在 t2 时刻保存 (t2 > t1)
        ↓
   时间差可能导致：
   • 2D 和 3D 地图范围不匹配
   • 定位时出现偏差
   • 导航规划错误
```

#### 3. **PGO 优化状态**
```
PGO 正在优化：
  [帧1] → [帧2] → [帧3] → [回环检测] → [优化中...]
                                            ↑
                                        此时保存
                                            ↓
                                    得到未优化完成的地图
                                    （精度不是最优）
```

---

## ✅ 保存流程说明

### 自动保存流程（推荐）

```
┌─────────────────────────────────────────────┐
│  用户点击 "保存地图"                         │
└─────────────────────────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│  系统检测到 SLAM 正在运行                    │
│  显示警告对话框：                             │
│  ┌────────────────────────────────────────┐ │
│  │ ⚠️ SLAM 正在运行                       │ │
│  │                                        │ │
│  │ 为确保数据完整性，建议先停止 SLAM。     │ │
│  │                                        │ │
│  │ 是否自动停止 SLAM？                     │ │
│  │                                        │ │
│  │  [ 是 ]  [ 否 ]                        │ │
│  └────────────────────────────────────────┘ │
└─────────────────────────────────────────────┘
         ↓               ↓
      选择"是"        选择"否"
         ↓               ↓
         │          取消保存
         │          (用户手动停止)
         ↓
┌─────────────────────────────────────────────┐
│  步骤 1: 自动停止 SLAM 组件                  │
│  • 检测运行中的 SLAM 类型                    │
│    - slam (简单模式)                         │
│    - slam_pgo (PGO优化模式)                 │
│  • 发送停止命令                              │
│  • 等待进程完全退出 (2秒)                    │
└─────────────────────────────────────────────┘
         ↓
┌─────────────────────────────────────────────┐
│  步骤 2: 保存地图                            │
│  • 显示进度对话框                            │
│  • 订阅 /map 话题 → 保存 2D 地图            │
│  • 复制 FastLIO 点云 → 保存 3D 地图         │
│  • 显示保存结果                              │
└─────────────────────────────────────────────┘
         ↓
┌─────────────────────────────────────────────┐
│  步骤 3: 询问是否重启 SLAM                   │
│  ┌────────────────────────────────────────┐ │
│  │ 地图已保存完成。                        │ │
│  │                                        │ │
│  │ 是否重新启动 SLAM PGO 继续建图？        │ │
│  │                                        │ │
│  │  [ 是 ]  [ 否 ]                        │ │
│  └────────────────────────────────────────┘ │
└─────────────────────────────────────────────┘
         ↓               ↓
      选择"是"        选择"否"
         ↓               ↓
   自动重启 SLAM      保持停止状态
```

### 手动保存流程

```
用户手动操作：
  1. 在控制面板停止 SLAM
  2. 点击 "保存地图"
  3. 系统检测无 SLAM 运行
  4. 直接进入保存流程
  5. 完成后手动启动 SLAM（如需继续建图）
```

---

## 🚀 使用方法

### 方式 1: 自动模式（推荐）

**适用场景**：正在建图，想保存当前地图

**步骤**：
1. 确保 SLAM 正在运行并已建立地图
2. 点击 "💾 保存地图" 按钮
3. 输入地图名称（如：`office_floor1`）
4. 在警告对话框中选择 **"是"** → 自动停止 SLAM
5. 等待保存进度完成
6. 在重启对话框中：
   - 选择 **"是"** → 自动重启 SLAM 继续建图
   - 选择 **"否"** → 保持停止状态

**优点**：
- ✅ 全自动流程，无需手动操作
- ✅ 确保数据完整性
- ✅ 可选继续建图

### 方式 2: 手动模式

**适用场景**：建图已完成，不需要继续

**步骤**：
1. 在控制面板手动停止 SLAM 组件
   - 点击 "SLAM (建图)" 或 "SLAM (建图-PGO优化)" 的 **"停止"** 按钮
2. 等待状态变为灰色（已停止）
3. 点击 "💾 保存地图" 按钮
4. 输入地图名称
5. 直接进入保存流程（无警告）
6. 完成后保持停止状态

**优点**：
- ✅ 完全控制流程
- ✅ 适合建图完成的场景

---

## 📊 保存进度详解

### 进度阶段说明

```
[====      ] 10%  检查系统状态
              ↓
              • 检查保存脚本是否存在
              • 检查/创建目标目录
              • 检查 /map 话题是否发布

[======    ] 20%  启动地图保存脚本
              ↓
              • 非阻塞启动子进程

[========  ] 30%  等待 2D 栅格地图数据...
     ↓
[==========] 55%  2D 地图保存成功
              ↓
              • 生成 .pgm 图像文件
              • 生成 .yaml 元数据文件

[==========] 60%  复制 3D 点云数据...
     ↓
[==========] 75%  正在复制 3D 点云...
     ↓
[==========] 90%  验证保存结果
              ↓
              • 检查所有文件是否生成
              • 计算文件大小

[==========] 100% 地图保存完成！
```

### 超时设置

| 阶段 | 超时时间 | 原因 |
|------|---------|------|
| **2D 地图** | 30 秒 | 等待 /map 话题数据 |
| **3D 地图** | 60 秒 | 复制大文件需要时间 |
| **总流程** | ~90 秒 | 包含所有阶段 |

---

## ❓ 常见问题

### Q1: 为什么保存超时？

**可能原因**：
1. **SLAM 正在运行** → 文件正在写入，无法完成复制
2. **点云文件非常大** → 超过 500MB，复制需要更长时间
3. **磁盘 I/O 慢** → 机械硬盘或 USB 存储

**解决方法**：
```bash
# 1. 确保 SLAM 已停止
ros2 node list | grep fastlio
# 应该没有输出

# 2. 检查点云文件大小
ls -lh ~/.ros/fastlio_maps/map_latest.pcd
# 如果 > 500MB，考虑优化地图或增加超时时间

# 3. 检查磁盘性能
sudo iotop
```

### Q2: 保存的 3D 地图为空或很小？

**可能原因**：
1. **SLAM 刚启动不久** → 点云数据还不够
2. **FastLIO 未正常保存** → `~/.ros/fastlio_maps/` 为空

**解决方法**：
```bash
# 检查 FastLIO 地图目录
ls -lh ~/.ros/fastlio_maps/

# 应该看到：
# map_latest.pcd  (几十 MB)
# map.pcd
# map_corrected.pcd (如果有 PGO)

# 如果为空，继续建图一段时间后再保存
```

### Q3: 2D 和 3D 地图不匹配？

**可能原因**：
- **SLAM 运行时保存** → 时间不一致
- **保存过程中移动了机器人** → 地图发生变化

**解决方法**：
- ✅ **始终停止 SLAM 后再保存**
- ✅ **使用自动停止功能**
- ✅ **保存前确保机器人静止**

### Q4: 如何验证地图保存成功？

**检查清单**：
```bash
# 进入地图目录
cd /home/ros/Code/Demo8/src/megarover3_ros2/megarover3_navigation/maps

# 检查文件
ls -lh my_map*

# 应该看到：
# my_map.yaml          163 B    (2D 元数据)
# my_map.pgm           25 KB    (2D 图像)
# my_map.pcd           53 MB    (3D 点云)
# my_map_corrected.pcd 27 MB    (PGO 优化版，可选)

# 验证 2D 地图
cat my_map.yaml
# 应该包含: image, resolution, origin

# 验证 3D 点云（使用 pcl_viewer）
pcl_viewer my_map.pcd
```

### Q5: 保存后如何使用地图？

**导航时使用**：
1. 在控制面板选择 **"导航 (PGO地图)"** 组件
2. 点击 **"启动"** 按钮
3. 第一次文件选择对话框：
   ```
   选择: .../maps/my_map.yaml
   ```
4. 第二次文件选择对话框：
   ```
   选择: .../maps/my_map.pcd
   ```
5. 等待定位初始化（通常几秒）
6. 开始导航

**命令行方式**：
```bash
ros2 launch megarover3_navigation fastlio2_pgo_navigation.launch.py \
  mode:=nav \
  map:=/home/ros/Code/Demo8/.../maps/my_map.yaml \
  pcd_map:=/home/ros/Code/Demo8/.../maps/my_map.pcd
```

---

## 🎯 最佳实践

### ✅ 推荐做法

1. **建图完成后保存**
   - 覆盖想要的区域
   - 闭合回环（走回起点）
   - 等待 PGO 优化完成（几秒钟）

2. **使用自动停止功能**
   - 点击保存时选择 "是"
   - 让系统自动处理停止/重启

3. **定期保存检查点**
   ```
   建图 10 分钟 → 保存 map_v1
   建图 20 分钟 → 保存 map_v2
   建图完成    → 保存 map_final
   ```

4. **命名规范**
   ```
   办公室：office_floor1
   仓库：  warehouse_area_a
   带日期：map_20260130
   带版本：office_v3
   ```

### ❌ 避免做法

1. ❌ **SLAM 运行时直接保存** → 数据可能不完整
2. ❌ **保存过程中移动机器人** → 地图变化
3. ❌ **不检查保存结果** → 可能文件损坏未发现
4. ❌ **覆盖重要地图** → 没有版本管理

---

## 📞 故障排查

### 错误信息对照表

| 错误消息 | 原因 | 解决方法 |
|---------|------|---------|
| **地图话题未发布** | SLAM 未运行 | 启动 SLAM 组件 |
| **2D 地图保存超时** | /map 话题无数据 | 检查 OctoMap 是否运行 |
| **3D 地图复制超时** | 文件太大或磁盘慢 | 等待更长时间或优化地图 |
| **未生成任何地图文件** | 脚本执行失败 | 查看详细日志 |
| **进程启动失败** | Python 环境问题 | 检查 ROS2 环境 |

### 日志位置

**控制面板日志**：
- 实时显示在日志查看器中

**ROS2 日志**：
```bash
# 查看最近的日志
ls -lt ~/.ros/log/

# 查看具体日志文件
cat ~/.ros/log/latest/complete_map_saver/stdout.log
```

**FastLIO 地图目录**：
```bash
# 检查 FastLIO 自动保存的地图
ls -lh ~/.ros/fastlio_maps/
```

---

## 🔄 版本历史

### v2.0 (2026-01-30)
- ✅ 添加 SLAM 运行检测
- ✅ 自动停止/重启 SLAM 功能
- ✅ 异步保存（不阻塞 UI）
- ✅ 实时进度显示
- ✅ 详细错误提示
- ✅ 保存结果可视化

### v1.0 (2026-01-29)
- 基础保存功能
- 2D + 3D 地图保存

---

## 📚 相关文档

- [PGO_MAPPING_GUIDE.md](PGO_MAPPING_GUIDE.md) - PGO 建图指南
- [CONTROL_PANEL_README.md](CONTROL_PANEL_README.md) - 控制面板完整说明
- [QUICK_START.md](QUICK_START.md) - 快速开始指南
